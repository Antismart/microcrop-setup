# MicroCrop Data Processor - Requirements
# Python 3.10+

# ============ Core Dependencies ============
python-dotenv==1.0.0
pydantic[email]==2.10.5
python-json-logger==2.0.7
typing-extensions==4.12.2

# ============ Async & HTTP ============
asyncio==3.4.3
aiohttp==3.9.1
# NOTE: Removed aiokafka - no longer using Kafka
aioredis==2.0.1
asyncpg==0.29.0
httpx==0.27.0  # UPDATED: Required for Planet Labs API client

# ============ Data Processing ============
numpy==1.26.3
pandas==2.1.4
scipy==1.11.4
scikit-learn==1.3.2
statsmodels==0.14.1

# ============ Geospatial (Simplified) ============
# NOTE: Removed rasterio, GDAL, opencv - no longer processing raw satellite images
shapely==2.0.2
pyproj==3.6.1

# ============ Database ============
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1
asyncpg==0.29.0

# ============ Redis & Caching ============
redis==5.0.1
hiredis==2.3.2

# ============ Message Queue & Workers ============
celery==5.3.6
kombu==5.3.4
flower==2.0.1
# NOTE: Removed kafka-python - no longer using Kafka streaming

# ============ Cloud Storage (NEW - for Planet Labs) ============
google-cloud-storage==2.14.0
# NOTE: Removed minio, boto3 - no longer storing images locally

# ============ Blockchain (Read-only) ============
# NOTE: Removed direct contract interaction - CRE handles blockchain writes
web3==7.7.0  # Keep for read-only queries only
# Note: eth-account, eth-utils, eth-typing, hexbytes are installed as web3 dependencies

# ============ Planet Labs Integration (NEW) ============
planet==2.7.0  # Planet Labs Python SDK
pandas>=2.1.4  # Required for CSV parsing (already included above)

# ============ IPFS ============
ipfshttpclient==0.8.0a2
requests==2.31.0

# ============ API & Web Framework ============
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
python-multipart==0.0.6
starlette==0.35.1

# ============ Authentication & Security (NEW) ============
python-jose[cryptography]==3.3.0  # JWT tokens for CRE authentication
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.1.2  # Password hashing backend

# ============ ML & Statistics ============
prophet==1.1.5
xgboost==2.0.3
lightgbm==4.5.0
catboost==1.2.7

# ============ Monitoring & Observability ============
prometheus-client==0.19.0
sentry-sdk==1.39.2
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-exporter-prometheus==1.12.0rc1

# ============ Testing ============
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-timeout==2.2.0
faker==22.3.0
factory-boy==3.3.0
hypothesis==6.96.1

# ============ Code Quality ============
black==23.12.1
flake8==7.0.0
mypy==1.8.0
pylint==3.0.3
isort==5.13.2
pre-commit==3.6.0

# ============ Documentation ============
mkdocs==1.5.3
mkdocs-material==9.5.5
mkdocstrings[python]==0.24.0

# ============ Development Tools ============
ipython==8.20.0
jupyter==1.0.0
jupyterlab==4.0.11
notebook==7.0.7
ipywidgets==8.1.1

# ============ Utilities ============
click==8.1.7
python-dateutil==2.8.2
pytz==2023.4
tenacity==8.2.3
pyyaml==6.0.1
orjson==3.9.12
ujson==5.9.0
